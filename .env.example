# Nexus AI Desktop Automation - Environment Variables

# Anthropic Claude API Key (optional for demo mode)
# Get your API key at: https://console.anthropic.com/
# Without this, the app will run in demo mode with simulated AI responses
ANTHROPIC_API_KEY=your_api_key_here

# Application Settings
NEXT_PUBLIC_APP_NAME=Nexus
NEXT_PUBLIC_APP_VERSION=0.1.0

# WebSocket Configuration (Phase 3 - Real-time features)
# Set to http://localhost:3001 for local dev with WebSocket server
# Leave empty to run without WebSocket (API-only mode)
NEXT_PUBLIC_WS_URL=http://localhost:3001
WS_PORT=3001

# WebSocket server URL for production (optional)
# NEXT_PUBLIC_WS_URL=https://your-ws-server.com
